import { moduleFor, test } from 'ember-qunit';

moduleFor('transform:tsygan', 'Unit | Transform | tsygan', {
});

test('it exists', function(assert) {
  let transform = this.subject();
  assert.ok(transform);
});
